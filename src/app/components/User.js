import React from 'react';

export const User = (props) => {
         return(
            <div>
                <h1>Main Page</h1>
                <p>Username: {props.username}</p>
            </div>
        );
 };